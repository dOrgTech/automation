/*
  Microservice that...
    1. Creates & posts proposals based on a template & arguments provided by the caller
    2. Stores the proposal ID
    3. Exposes query endpoints enabling callers to get the proposal's state (pending, passed, failed)
*/
